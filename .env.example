# ICDA Prototype - Environment Configuration
# Copy this to .env and fill in your values
#
# LITE MODE: Leave AWS blank to run without AI features
# FULL MODE: Add AWS creds for Bedrock Nova AI

# ================================================
# AWS Configuration (OPTIONAL - for AI features)
# ================================================
# Leave blank to run in LITE MODE (no AI, but app still works)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Bedrock Model (only used if AWS configured)
NOVA_MODEL=us.amazon.nova-micro-v1:0

# ================================================
# Redis Cache (OPTIONAL)
# ================================================
# Leave blank to use in-memory cache
# For local Docker: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ================================================
# OpenSearch Vector Store (OPTIONAL)
# ================================================
# Leave blank to use keyword search fallback
# For local Docker: localhost:9200
OPENSEARCH_HOST=localhost:9200
OPENSEARCH_INDEX=customers

# ================================================
# For Docker Compose internal networking
# ================================================
# REDIS_URL=redis://redis:6379
# OPENSEARCH_HOST=opensearch:9200
