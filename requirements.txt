# Core - required
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3
python-dotenv==1.0.1
python-multipart==0.0.9

# AWS (optional - for AI features)
boto3==1.35.86

# AWS Bedrock AgentCore Memory (optional - for advanced memory features)
# bedrock-agentcore>=0.1.0
# bedrock-agentcore-starter-toolkit>=0.1.0
# Note: Uncomment when packages are available. Until then, local fallback is used.

# Redis (for distributed caching)
redis[hiredis]==5.2.1

# OpenSearch (for vector search)
opensearch-py[async]==3.1.0

# File watching (for auto-indexing knowledge documents)
watchdog==4.0.0

# Document parsing (for various file format support)
pypdf==6.4.0
python-docx==1.1.2
openpyxl==3.1.5

# Gemini Enforcer (optional - for AI quality enforcement)
google-genai>=1.0.0
httpx>=0.27.0

# Testing utilities (optional)
rich>=13.0.0